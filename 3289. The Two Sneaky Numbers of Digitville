class Solution:
    def getSneakyNumbers(nums):
        arr_check =[]
        two_nums = []
        for i in range(0, len(nums)):
            if nums[i] in arr_check:
                two_nums.append(nums[i])
            if len(two_nums)==2:
                return two_nums
            arr_check.append(nums[i])
