class Solution:
    def kthFactor(n, k):
        arr_list = []
        for i in range(1, n+1):
            if n%i == 0:
                arr_list.append(i)
        try:
            return arr_list[k-1]
        except:
            return -1
